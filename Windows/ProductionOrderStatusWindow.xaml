<Window x:Class="LogisticsWPF.Windows.SupplyOrderStatusWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Смена статуса заказа на поставку"
        Width="380"
        Height="Auto"
        SizeToContent="Height"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{StaticResource BackgroundColor}"
        FontFamily="{StaticResource AppFont}">

    <Border Padding="20">
        <StackPanel>
            <TextBlock Text="Смена статуса"
                       FontSize="20"
                       FontWeight="Bold"
                       Foreground="{StaticResource PrimaryColor}"
                       Margin="0,0,0,15"/>

            <TextBlock Text="Текущий статус:" FontWeight="SemiBold"/>
            <TextBlock x:Name="CurrentStatusText"
                       FontSize="14"
                       Margin="5,2,0,15"/>

            <TextBlock Text="Выберите новый статус:" FontWeight="SemiBold"/>
            <ComboBox x:Name="StatusComboBox"
                      DisplayMemberPath="StatusName"
                      SelectedValuePath="StatusID"
                      Margin="0,5,0,25"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Сохранить"
                        Width="120"
                        Height="35"
                        Click="Save_Click"
                        Background="{StaticResource AccentColor}"/>
                <Button Content="Отмена"
                        Width="120"
                        Height="35"
                        Margin="10,0,0,0"
                        Click="Cancel_Click"
                        Background="#B0B9C2"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
